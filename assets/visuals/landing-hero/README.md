Landing Hero Assets

Files added:
- `prompt.md` — final prompt and generator guidance
- `negative_prompt.txt` — negative prompt to avoid artifacts and unwanted subjects
- `variants.md` — aspect ratio and export recommendations
- `placeholder.svg` — simple placeholder graphic you can use in the meantime

How to generate

1. Use your preferred image generation service (Stable Diffusion, Midjourney, DALL·E, etc.).
2. Paste the prompt in `prompt.md` and the negative prompt in `negative_prompt.txt`.
3. Generate at the recommended resolution (see `variants.md`) and save as PNG.

If you want, provide API credentials and I can attempt a single-generation run for review.
